# Linghe-Core Sandbox (skeleton)

**Purpose**  
Reproduce the 48 h identity-drift test for the Linghe-Core Personality Module.

## Quick start
```bash
docker compose up    # pulls stub image, starts scheduler + heartbeat
